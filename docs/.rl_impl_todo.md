# RL Pipeline Implementation TODO (from docs/07_rl_pipeline_design.md)

## Wave 1 — Stage A repairs (parallel subagents)  ✅ DONE
- [x] W1-A "runner-core": V2 parse-fail, V1 asap7 units + quarantine jsonl, V4 penalty ladder,
      V8 abc aliasing, V3+V11 RTL-hash variant names, V10 timeout/process-group/no-memoise-fail,
      Stage-C refactors (run_*_optimizer TIMEOUT logging, run_cascade_optimizer.py:109 crash),
      wire UCB bounds in run scripts.
      Files: physical_runner.py, physical_reward.py (NOT cycle-fit lines), cascade_reward.py,
      run_physical_optimizer.py, run_cascade_optimizer.py, physical/orfs/make/asap7/tinymac_accel/constraint.sdc
- [x] W1-B "small-knobs": V5 max_speedup=1024 (search_space_full.yaml), V7 forward all knobs
      (physical_env.py), V9 UCB bounds param + env.reward_bounds attrs, V14 sweep.sh hardening.
      Files: search_space_full.yaml, physical_env.py, agents/ucb_agent.py, env.py, cascade_env.py, sweep.sh
- [x] W1-C "sim-truth" (AVG_CYCLES: L1=273130 L2=152140 L4=91650 L8=61400 L16=46670 L32=39310; fit 31452.5+241567/lanes; MAX_SPEEDUP_GRID stays 576): V13 per-chunk saturation in sim_main.cpp (match RTL), rebuild sim,
      then V6 measure & re-pin: optimizer/constants.py (SW_BASELINE_CYCLES, AVG_CYCLES{1,2,4,8,16,32},
      behavioral_cycles fit, MAX_SPEEDUP), extend measure_real.py, update benchmark_agents.py:47,
      cascade.py:49, physical_reward.py cycle-fit line ONLY.
- [x] W1-D "model-truth" (benign drift from cd593f1 layout fix → tolerance ±3 LSB, 64/64): V12 host test 63/64 investigation → tolerance ±3 LSB or real regression;
      firmware/tinyengine_port only.

## Wave 2 — Review (parallel subagents)  ✅ DONE
- [x] W2-A: all PASS; fixed `?=`→`=` SYNTH_REPEATABLE_BUILD in physical_runner.py. Mock e2e + all tests pass.
- [x] W2-B: all PASS; verified V13 vs RTL FSM (incl. partial chunk, K=0, bias); re-derived MAX_SPEEDUP_GRID
      (479.8x, 20% headroom); fixed stale derivation comment + CLAUDE.md numbers. Residual: acc16 accuracy
      now LANES-dependent (47-58/64), benchmark uses 47/64 — acknowledged.

## Pinned interfaces for Wave 3 (all agents code against these)
- FunnelEnv (optimizer/funnel.py): reset(config)->state22, step(action)->(state,reward,done,info);
  ACTIONS=["kill","re-proxy","promote","commit"]; fidelities F0(0s)/F1(5s)/F2(45s)/F3(420s)/F4;
  table=None live | dict table-sim; logs rows to results_funnel.jsonl
- State 22-dim: [0-4]=x(lanes_log2/5, (acc-16)/16, (clk-3)/5, recipe_idx/2, platform), [5-6]=F0(cycles_norm,acc),
  [7-8]=F1, [9-13]=F2(area_norm,wns,ff,cells,levels), [14-15]=surrogate mu/sigma, [16]=incumbent/4.5,
  [17]=budget_frac, [18-21]=depth one-hot
- Surrogate (optimizer/surrogate.py): fit(rows), predict(x, obs=None)->{metric:(mu,sigma)};
  METRICS=["area_um2","period_ns","power_mw"]
- PromotionAgent (optimizer/agents/promotion_agent.py): LinUCB; act(state)->action, update(state,action,reward)
- Recipe (optimizer/recipe.py): RECIPES=("orfs_speed","orfs_area","plain"); run_physical(..., abc_recipe=),
  run_synth_sta(..., abc_recipe=); variant_name: orfs_speed=no suffix, _area, _plain
- search_space_funnel.yaml: lanes{1,2,4,8,16,32}, acc_w{16,24,32}, clk cont [3,8] n45 (table grid 0.5),
  recipe{3}, util=40/density=0.60 fixed (W3-C owns the file)

## Wave 3 — Stage B infrastructure  ✅ DONE
- [x] W3-A recipe.py: proxy areas bit-identical to doc (14456/17187/20675); plain = proxy-only (ORFS
      synth_preamble.tcl hardcodes script choice); run_physical raises on plain
- [x] W3-B surrogate.py: CP3 PASS on 44 mined builds — rho area .895 / period .865 / power .95;
      model saved surrogate_n45.joblib; fit_surrogate.py vendors report regexes
- [x] W3-C funnel.py + search_space_funnel.yaml (594 configs); self-tests pass (table + mock live)
- [x] W3-D promotion_agent.py (LinUCB/FixedGate/Random) + build_table.py (strategic=84 cfgs ~1.06h)
      + benchmark_funnel.py (selftest pass, but ran vs fallback synthetic table — needs re-run vs real FunnelEnv)

## Known Wave-3 seams for W4 review
- funnel.py maps plain→orfs_speed at F3 silently vs recipe.py raising — reconcile + record effective recipe in obs
- benchmark_funnel selftest must run against REAL funnel.load_table/FunnelEnv table mode
- surrogate not yet wired into FunnelEnv state slots 14-15 (W3-D used 0.0/1.0 sentinels)
- W2-B residual: SW_BASELINE_CYCLES duplicated in reward.py/runner.py/dashboard.py — import from constants.py

## Wave 4 — Review + integration  ✅ DONE
- [x] W4-A: 8 integration bugs found+fixed (build_table canonical key names; FixedGate WNS threshold
      normalization; recipe_suffix fallback alias; effective_abc_recipe recording; SW_BASELINE imports
      x3; benchmark refactored to drive real FunnelEnv(table=...)). Full test matrix green.
- [x] CP1 (mock half): zero positive rewards from None metrics — verified Wave 2 + Wave 4
- [x] CP1 (real half): asap7 @1.0ns PASS — status ok, GDS produced (9 min), wns −0.965 ns (sane scale,
      was −1861), area 1433 µm², Fmax 509 MHz, variant L4_A24_c1_rb8d8aaa8 (new naming visible)
- [x] CP2: benchmark_agents reproduces {4,24,5}, reward 3.9952
- [x] CP3: surrogate 5-fold rho = .895 area / .865 period / .95 power (n=44) — PASS
- [x] CP4 (synthetic table, 20 seeds): fixed-gate med 0.59h/90% > random 1.09h/85% > linucb cold-start 50%
      (honest negative for cold LinUCB; real table + pretraining = next step)
- [x] Real strategic table build COMPLETE: 252 rows (84 configs x F0/F1/F2), 0 errors, 72.6 min.
      F1 rows regenerated via real sim after kwarg fix (acc_w→acc_width; accuracy via funnel._f0_accuracy).
      Validated: load_table → FunnelEnv episode w/ real surrogate (state slots populate, F2 obs real,
      missing-F3 commit pays −20). Recipe spread in real data matches doc bit-for-bit.

## Remaining future work (post-implementation, from doc Phase 6)
- Full 594-config F2 table (~7h, resumable: build_table.py --out results_funnel.jsonl)
- F3 rows for top candidates (~7 min each) → real CP4 benchmark w/ LinUCB pretraining
- CP5: one 4-hour live campaign vs table-simulated trajectory
- asap7 F4 transfer sweep (~12 configs @ 0.8–1.2 ns) — unblocked by V1 fix, first run validated

## Final
- [ ] Full report to user
